<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
</head>

<body>




    <form action="/updateuserprofiledetail" method="post" enctype="multipart/form-data">
        <div class="backbtn">
            <button onclick="goto('/editprofile')">
                🡠
            </button>
        </div>
        <h2>Edit Profile</h2>
        <label for="photo">Profile Photo:</label>
        <input type="file" id="photo" name="image">
        <label for="username">Username:</label>
        <input type="text" id="username" name="username" value="<%= user.username %>" required>
        <label for="about">About:</label>
        <textarea id="about" name="about" rows="4" required><%= user.about %></textarea>
        <input type="submit" value="Save">
    </form>
</body>

</html>
<script>
    function goto(link) {
        window.location.href = link;
    }
</script>
<style>
    * {
        box-sizing: border-box;
        font-family: math;

    }

    img {
        opacity: .4;
    }

    .backbtn button {
        top: 2%;
        left: -7%;
        font-size: 27px;
        cursor: pointer;
        border: none;
        width: 40px;
        background-color: #65937c;
        color: white;
        border-radius: 200px;
        position: absolute;
    }
    .backbtn button:hover{
        background-color: #5e7c6d;
    }

    body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        flex-direction: column;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: beige;
    }

    form {
        position: relative;
        width: 400px;
        background-color: #fff;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }



    h1 {
        cursor: pointer;
        color: #7cbc9c;
    }

    h2 {
        text-align: left;
        /* margin-bottom: 16px; */
        margin: 0;
        /* border-bottom: 1px solid gray; */

        margin-bottom: 10px;
        color: #333;
    }

    label {
        display: block;
        margin-bottom: 10px;
        color: #555;
    }

    input[type="text"],
    input[type="file"],
    textarea {
        width: 100%;
        padding: 8px;
        margin-bottom: 20px;
        border: 1px solid #ccc;
        resize: none;
        outline: none;
        border-radius: 5px;
    }

    textarea {
        height: 120px;
    }

    input[type="submit"] {
        width: 100%;
        padding: 10px;
        border: none;
        border-radius: 5px;
        background-color: #7cbc9c;
        color: #fff;
        font-size: 16px;
        cursor: pointer;
    }

    input[type="submit"]:hover {
        background-color: #65937c;
    }
</style>